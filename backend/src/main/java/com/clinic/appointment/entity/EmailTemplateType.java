package com.clinic.appointment.entity;

public enum EmailTemplateType {
    BOOKING_CONFIRMATION,
    REMINDER,
    CANCELLATION,
    LEAVE_CANCELLATION,
    MEDICATION_REMINDER,
    POST_VISIT_SUMMARY,
    RESCHEDULED
}
